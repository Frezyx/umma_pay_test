import 'package:flutter/material.dart';

class DesignThemeShadows {
  final box = <BoxShadow>[
    BoxShadow(
      color: Colors.black.withOpacity(0.03),
      blurRadius: 20.0,
      spreadRadius: 2.0,
      offset: Offset(
        0.0,
        3.0,
      ),
    )
  ];
}
